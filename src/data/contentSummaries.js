/**
 * Summary schemas retained for the dormant Articles and Ideas routes.
 * `postSummaries` is regenerated by `npm run data` when posts are added.
 */
export const researchPaperSummaries = [];

export const postSummaries = [

];
